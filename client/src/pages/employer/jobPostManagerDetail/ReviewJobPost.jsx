import React from "react";
import ModalReviewJob from "../jobPost/ModalReviewJob";

const ReviewJobPost = ({ data }) => {
  return <ModalReviewJob jobData={data} />;
};

export default ReviewJobPost;
